image: "fedora:27"

variables:
  GIT_SUBMODULE_STRATEGY: recursive

before_script:
  - dnf -y install clang cmake openssl-static.i686 boost-static.i686 zlib-static.i686 boost-devel openssl-devel

build:
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make